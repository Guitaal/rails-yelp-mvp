<div class="container mt-5">
  <h1>Index</h1>
  <ul>
    <% @restaurants.each do |restaurant| %>
      <li><%= link_to "#{restaurant.name}", restaurant_path(restaurant)%></li>
    <% end %>
  </ul>
  <%= link_to "Add restaurant", new_restaurant_path, class: "btn btn-primary mt-5"%>
</div>
